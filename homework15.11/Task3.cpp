/*#include <iostream>
#include "string.h"

using namespace std;

bool fMoreS(char firstString[], char secondString[]);
bool simpleEquals(char firstString[], char secondString[]);

int main()
{
	const int N = 256;
	char firstString[] = { 0 };
	cin.getline(firstString, N, '\n');
	char secondString[] = { 0 };
	cin.getline(secondString, N, '\n');
	bool m = simpleEquals(firstString, secondString);
	if (m)
		cout << "Equals";
	if (fMoreS(firstString, secondString))
		cout << "The first string more than the second";
	cout << "The first string less than the second";
	system("pause");
}*/
